#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
// char c[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
// unsigned int i;
for (char i = 'a'; i <  'z'; i++)
{
_putchar(i);
}
_putchar('\n');

return (0);
}
